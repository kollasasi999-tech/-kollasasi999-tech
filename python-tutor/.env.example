# Copy to .env (or just export these in your shell) and fill in your key.
# Get a key at https://console.anthropic.com/

ANTHROPIC_API_KEY=sk-ant-...

# Optional: default model without passing --model each time.
# One of: opus | sonnet | haiku   (haiku is the cheapest)
# TUTOR_MODEL=opus
